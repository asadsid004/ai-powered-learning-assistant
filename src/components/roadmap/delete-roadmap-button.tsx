"use client";

import React from "react";
import { Button } from "../ui/button";

const deleteRoadmapButton = ({
  onClickFunction,
}: {
  onClickFunction: (id: string) => void;
}) => {
  return <Button>deleteRoadmapButton</Button>;
};

export default deleteRoadmapButton;
